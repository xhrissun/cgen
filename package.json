{
  "name": "contract-management-system",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node start.js",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  }
}